name: Test results and Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Verifica el repositorio
      - name: Check out the repository
        uses: actions/checkout@v4

      # 2. Configura Node.js
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18 # Asegúrate de usar la versión adecuada

      # 3. Instala las dependencias
      - name: Install dependencies
        run: npm install

      # 4. Ejecuta las pruebas con cobertura
      - name: Run tests with coverage
        run: npx jest --coverage

      # 5. Genera un archivo comprimido con los resultados de cobertura
      - name: Package coverage report
        run: |
          mkdir -p artifacts
          zip -r artifacts/coverage-report.zip coverage/

      # 6. Sube los resultados de cobertura como artefacto
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: artifacts/coverage-report.zip

      # 7. Sube los resultados de cobertura a Codecov
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov-report/* # Ruta a los reportes de cobertura generados por Jest
          token: ${{ secrets.CODECOV_TOKEN }} # Elimina esta línea para repos públicos
          fail_ci_if_error: true # Falla el flujo si Codecov encuentra un problema

      # 8. Sube los resultados de pruebas a Codecov
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      # 9. Inicia sesión en Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 10. Configura Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
